<!DOCTYPE html>
<html lang="tr">
<head>
<meta charset="UTF-8">
<title>Helin&İlyas</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
@import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600&family=Montserrat:wght@300;400&display=swap');

* { margin:0; padding:0; box-sizing:border-box; }

/* ✅ MOBİL UYUMLU BODY (EKLENDİ) */
html, body {
    width: 100%;
}

body {
    min-height: 100svh; /* mobilde doğru yükseklik */
    background: radial-gradient(circle at top, #fff6e5, #f2e4c9);
    display:flex;
    align-items:center;
    justify-content:center;
    font-family:'Montserrat', sans-serif;
    overflow-y:auto;   /* ⬅️ aşağı kaydırma açıldı */
    overflow-x:hidden;
    padding: 40px 0;
}

/* 🌤 Işık */
.light {
    position:absolute;
    width:90vw;               /* ✅ mobil uyum */
    height:90vw;
    max-width:600px;
    max-height:600px;
    background:radial-gradient(circle, rgba(255,255,255,0.8), transparent 70%);
    animation:pulse 6s ease-in-out infinite;
    transition: transform 0.3s ease;
    pointer-events:none;
}

@keyframes pulse {
    0% { transform:scale(1); opacity:0.7; }
    50% { transform:scale(1.05); opacity:1; }
    100% { transform:scale(1); opacity:0.7; }
}

/* ❤️ Kalpler */
.heart {
    position:absolute;
    font-size:14px;
    color:rgba(200,120,120,0.25);
    animation:floatUp linear forwards;
    user-select:none;
    pointer-events:none; /* ✅ sayfayı kilitlemesin */
}

@keyframes floatUp {
    from { transform:translateY(0) scale(0.8); opacity:0; }
    20% { opacity:1; }
    to { transform:translateY(-120vh) scale(1.1); opacity:0; }
}

/* 🖤 İçerik */
.container {
    text-align: center;
    position: relative;
    z-index: 2;
    width: 100%;
    max-width: 900px;
    padding: 0 20px;
}

.name {
    font-family:'Playfair Display', serif;
    font-size:4.5rem;
    letter-spacing:5px;
    color:#3a2a1a;
    text-shadow:0 0 25px rgba(255,255,255,0.9);
    animation:fadeIn 3s ease forwards;
}

.meaning {
    margin-top:14px;
    font-size:1rem;
    color:#6b5a46;
    opacity:0;
    animation:fadeIn 3s ease forwards;
    animation-delay:1s;
}

.timer {
    margin-top:26px;
    font-size:1.1rem;
    color:#3a2a1a;
    opacity:0.85;
}

.dynamic-text {
    margin-top:18px;
    font-size:0.95rem;
    color:#7a664f;
    transition:opacity 1s ease;
}

@keyframes fadeIn {
    from { opacity:0; transform:translateY(10px); }
    to { opacity:1; transform:translateY(0); }
}

/* ✅ MOBİL ÖZEL AYAR (EKLENDİ) */
@media (max-width: 768px) {
    .name {
        font-size: 2.3rem;
        letter-spacing: 2px;
    }

    .timer {
        font-size: 0.95rem;
        margin-top: 18px;
    }

    .meaning,
    .dynamic-text {
        font-size: 0.85rem;
        line-height: 1.5;
    }
}
</style>
</head>

<body>

<div class="light" id="light"></div>

<div class="container">
    <div class="name">Helin&İlyas</div>
    <div class="meaning">"Benim adım senin peşinden gelmek için, senin adın ise bana yol göstermek için var. İyi ki varsın ışığım..."</div>
    <div class="timer" id="timer">yükleniyor...</div>
    <div class="dynamic-text" id="dynamicText"></div>
</div>

<script>
/* ⏳ Sayaç */
const startDate = new Date("2026-01-22T20:30:00");

function updateTimer() {
    const now = new Date();
    const diff = now - startDate;

    const days = Math.floor(diff / (1000*60*60*24));
    const h = Math.floor((diff / (1000*60*60)) % 24);
    const m = Math.floor((diff / (1000*60)) % 60);
    const s = Math.floor((diff / 1000) % 60);

    document.getElementById("timer").innerText =
        `${days} gün ${h} saat ${m} dakika ${s} saniye`;

    const text = document.getElementById("dynamicText");
    if (days < 30) {
        text.innerText = "Önce ışığımı buldum. Zaman durana kadar seni kaybetmeyeceğim.";
    } else if (days < 90) {
        text.innerText = "Zaman geçtikçe daha da parladı.";
    } else {
        text.innerText = "Artık karanlık diye bir şey yok.";
    }
}
setInterval(updateTimer, 1000);
updateTimer();

/* ❤️ Soft kalpler */
function createHeart(){
    const heart=document.createElement("div");
    heart.className="heart";
    heart.innerHTML="❤";
    heart.style.left=Math.random()*100+"vw";
    heart.style.bottom="-20px";
    heart.style.fontSize=(10+Math.random()*12)+"px";
    heart.style.animationDuration=(10+Math.random()*8)+"s";
    document.body.appendChild(heart);
    setTimeout(()=>heart.remove(),20000);
}
setInterval(createHeart,900);

/* 🖱 Mouse ışık tepkisi */
const light = document.getElementById("light");
document.addEventListener("mousemove", e => {
    const x = (e.clientX / window.innerWidth - 0.5) * 40;
    const y = (e.clientY / window.innerHeight - 0.5) * 40;
    light.style.transform = `translate(${x}px, ${y}px)`;
});
</script>

</body>
</html>
